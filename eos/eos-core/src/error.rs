//! Error trait bounds and core diagnostic structures.
//!
//! Because `eos-core` is backend-agnostic, traits define error bounds
//! via associated types rather than concrete library-wide error enums.
