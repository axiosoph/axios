[package]
edition = "2024"
name    = "ion-eos"
version = "0.1.0"

[dependencies]
capnp     = "0.20"
capnp-rpc = "0.20"
eos-proto = { path = "../../eos/eos-proto" }
thiserror = "2"
tokio     = { version = "1", features = ["net", "rt"] }
